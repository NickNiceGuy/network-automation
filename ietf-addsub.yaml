- name: Playbook per creazione e descrizione Subinterface via NETCONF
  hosts: NE8000-F1A
  connection: netconf
  gather_facts: no

  tasks:
    - name: Crea subinterface 25GE0/1/42.100, descrizione e VLAN
      ansible.netcommon.netconf_config:
        target: candidate
        content: |
          <config xmlns="urn:ietf:params:xml:ns:netconf:base:1.0">
            <!-- Utilizziamo esclusivamente il namespace IFM funzionante -->
            <ifm xmlns="urn:huawei:yang:huawei-ifm">
              <interfaces>
                <interface>
                  <name>25GE0/1/42.100</name>
                  <class>sub-interface</class>
                  <type>25GE</type>
                  <description>Subint creata da Semaphore UI</description>
                  
                  <!-- Struttura ad albero interna per il tag della VLAN -->
                  <dot1q-termination>
                    <vlan-id>100</vlan-id>
                  </dot1q-termination>
                  
                </interface>
              </interfaces>
            </ifm>
          </config>
      register: subint_result

    - name: Mostra esito creazione
      debug:
        var: subint_result







