[defaults]
inventory = ./inventory.ini
host_key_checking = False
retry_files_enabled = False

[persistent_connection]
# Aumenta il timeout per comandi complessi o sessioni SSH lente
connect_timeout = 60
command_timeout = 60
